@using CloneHabr.Dto.Requests;
@inject HttpClient httpClient

@foreach (var item in articlesLidResponses)
{
    <ArticleLid articleDto="@item" />
    <hr />
    
}


@code {
    [Parameter]
    public List<ArticleDto> articlesLidResponses { get; set; }
    
    protected override async void OnParametersSet()
    {
        Console.WriteLine("*** running ArticleLidsList.OnParametersSet() ***");
    }
}
